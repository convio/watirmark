Demo of watirmark framework
Going to automate twitter here, now in 5 minutes
Start with test cases
  Don't need framework to start, just need to know the feature
  Beauty is that this file is what we'll run to run our tests

watirmark twitter
  -  much like ruby on rails, create a scaffold of a new test project
(1) run sample to make sure we're connected
(2) copy over feature file and run with failure
(3) get steps and create step definition file
(4) run to see things in pending
(5) edit query to add the
    - search
    - submit
(6) run to show query execute
(7)

make fail (forget /i)
look at HTML output


run rake task (can I save to file without having to do that?)

